# Puerto del motor Genesis ID (por defecto 4000)
PORT=4000
# Ruta del archivo de persistencia (por defecto ./data/genesis.json)
# GENESIS_DATA_FILE=./data/genesis.json

# ---- Puente con el portal oficial Genesis ID (genesisid.online) ----
# La API key vive SOLO aquí, nunca dentro de la app (un APK se descomprime).
GENESIS_PORTAL_API=https://www.genesisid.online
GENESIS_API_KEY=gid_live_pon_aqui_tu_clave
