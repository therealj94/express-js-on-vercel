# Secreto para firmar los tokens de sesión (JWT). Usa un valor largo y aleatorio en producción.
JWT_SECRET=change-me-in-production

# Puerto local del servidor Express (opcional, por defecto 3001)
PORT=3001
